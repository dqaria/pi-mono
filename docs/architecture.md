# pi-mono 项目架构文档

> 版本: 0.51.5 | 类型: npm workspaces monorepo | 运行时: Node.js >= 20.0.0

---

## 一、项目完整架构总览

```
+===================================================================+
|                        pi-mono (Monorepo)                         |
|                  npm workspaces · TypeScript · ESM                |
+===================================================================+
|                                                                   |
|  +-----------------------+    用户交互层 (User-Facing)            |
|  |                       |                                        |
|  |  +---------------+   |   +---------------+   +-------------+  |
|  |  | pi-coding-    |   |   |   pi-mom      |   |  pi-web-ui  |  |
|  |  | agent         |   |   |  (Slack Bot)  |   | (Web 组件)  |  |
|  |  | (主 CLI: pi)  |   |   |  (CLI: mom)   |   |             |  |
|  |  +-------+-------+   |   +-------+-------+   +------+------+  |
|  |          |            |           |                  |         |
|  |          |            |           |                  |         |
|  +-----------------------+           |                  |         |
|             |                        |                  |         |
|             v                        v                  |         |
|  +--------------------+  +--------------------+        |         |
|  |  pi-agent-core     |  |  pi-agent-core     |        |         |
|  |  (Agent 运行时)    |  |                    |        |         |
|  +--------+-----------+  +--------+-----------+        |         |
|           |                       |                     |         |
|           v                       v                     v         |
|  +--------------------------------------------------------+      |
|  |                      pi-ai                              |      |
|  |         (统一多厂商 LLM API 抽象层)                     |      |
|  +--------------------------------------------------------+      |
|                                                                   |
|  +------------------+   +------------------+                      |
|  |    pi-tui        |   |    pi-pods       |                      |
|  |  (终端 UI 库)    |   |  (GPU Pod 管理)  |                      |
|  +------------------+   +--------+---------+                      |
|                                  |                                |
|                                  v                                |
|                         pi-agent-core                             |
+===================================================================+
```

---

## 二、模块分层架构图

```
+===================================================================+
|                          分层架构                                  |
+===================================================================+

  层级 1: 应用层 (Applications)
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  ┌─────────────────┐  ┌──────────────┐  ┌──────────────────┐   │
  │  │ pi-coding-agent │  │   pi-mom     │  │    pi-web-ui     │   │
  │  │                 │  │              │  │                  │   │
  │  │ ·交互式编程终端 │  │ ·Slack 聊天  │  │ ·Web Components  │   │
  │  │ ·工具系统       │  │  机器人      │  │ ·AI 聊天界面     │   │
  │  │ ·会话管理       │  │ ·消息代理    │  │ ·文档预览        │   │
  │  │ ·扩展系统       │  │ ·沙箱执行    │  │                  │   │
  │  │ ·多 UI 模式     │  │              │  │                  │   │
  │  └────────┬────────┘  └──────┬───────┘  └────────┬─────────┘   │
  │           │                  │                    │             │
  └───────────┼──────────────────┼────────────────────┼─────────────┘
              │                  │                    │
  层级 2: Agent 运行时层 (Agent Runtime)
  ┌───────────┼──────────────────┼────────────────────┼─────────────┐
  │           v                  v                    │             │
  │  ┌──────────────────────────────────────┐         │             │
  │  │          pi-agent-core               │         │             │
  │  │                                      │         │             │
  │  │  ·Agent 生命周期管理                 │         │             │
  │  │  ·传输层抽象 (Transport)             │         │             │
  │  │  ·状态管理 (State)                   │         │             │
  │  │  ·附件支持 (Attachments)             │         │             │
  │  │  ·Agent 执行循环 (Loop)              │         │             │
  │  └──────────────────┬───────────────────┘         │             │
  │                     │                             │             │
  └─────────────────────┼─────────────────────────────┼─────────────┘
                        │                             │
  层级 3: AI 基础设施层 (AI Infrastructure)
  ┌─────────────────────┼─────────────────────────────┼─────────────┐
  │                     v                             v             │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │                        pi-ai                             │   │
  │  │                                                          │   │
  │  │  ·统一 LLM API 接口                                     │   │
  │  │  ·多厂商 Provider 适配                                   │   │
  │  │  ·流式响应处理 (Streaming)                               │   │
  │  │  ·模型注册与发现 (Model Registry)                        │   │
  │  │  ·API Key 管理                                           │   │
  │  │  ·JSON Schema 验证                                       │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘

  层级 4: UI 基础库 (UI Foundations)    独立工具 (Standalone Tools)
  ┌──────────────────────────────┐     ┌──────────────────────────┐
  │  ┌────────────────────────┐  │     │  ┌────────────────────┐  │
  │  │        pi-tui          │  │     │  │     pi-pods        │  │
  │  │                        │  │     │  │                    │  │
  │  │  ·差分渲染引擎         │  │     │  │  ·vLLM 部署管理   │  │
  │  │  ·终端组件库           │  │     │  │  ·GPU Pod 管控     │  │
  │  │  ·按键绑定系统         │  │     │  │  ·SSH 连接管理     │  │
  │  │  ·自动补全             │  │     │  │  ·模型配置         │  │
  │  │  ·Markdown 渲染        │  │     │  │                    │  │
  │  └────────────────────────┘  │     │  └────────────────────┘  │
  └──────────────────────────────┘     └──────────────────────────┘
```

---

## 三、内部依赖关系图

```
  ┌────────────────────────────────────────────────────────────────┐
  │                   内部包依赖关系 (DAG)                         │
  └────────────────────────────────────────────────────────────────┘

                        pi-mom
                        (Slack Bot)
                          │
                          │ 依赖
                          v
                    pi-coding-agent ─────────────────────┐
                    (编程 Agent)                         │
                     │        │                          │
                     │        │ 依赖                     │ 依赖
                     │        v                          v
                     │   pi-agent-core            pi-tui
                     │   (Agent 运行时)           (终端 UI)
                     │        │
                     │        │ 依赖
                     v        v
                      pi-ai
                   (LLM API)

  ┌─────────────────┐          ┌─────────────────┐
  │    pi-web-ui    │          │    pi-pods       │
  │   (Web 组件)    │          │  (GPU Pod CLI)   │
  │    │       │    │          │       │          │
  │    │       │    │          │       │ 依赖     │
  │    v       v    │          │       v          │
  │ pi-ai   pi-tui │          │  pi-agent-core   │
  └─────────────────┘          └─────────────────┘

  =============================================================
  依赖矩阵:
  =============================================================
  包名                 依赖的内部包
  ---                  ---
  pi-tui               (无 — 独立基础库)
  pi-ai                (无 — 独立基础库)
  pi-agent-core        pi-ai
  pi-coding-agent      pi-ai, pi-agent-core, pi-tui
  pi-mom               pi-ai, pi-agent-core, pi-coding-agent
  pi-web-ui            pi-ai, pi-tui
  pi-pods              pi-agent-core
  =============================================================
```

---

## 四、构建顺序

```
  构建流水线 (串行依赖链):

  ┌────────┐   ┌────────┐   ┌──────────────┐   ┌──────────────────┐
  │ pi-tui │──>│ pi-ai  │──>│pi-agent-core │──>│ pi-coding-agent  │
  └────────┘   └────────┘   └──────────────┘   └────────┬─────────┘
                                                        │
                                              ┌─────────┼─────────┐
                                              v         v         v
                                          ┌───────┐ ┌────────┐ ┌───────┐
                                          │pi-mom │ │pi-web- │ │pi-pods│
                                          │       │ │  ui    │ │       │
                                          └───────┘ └────────┘ └───────┘

  实际 build 命令执行顺序:
  1. pi-tui
  2. pi-ai
  3. pi-agent-core
  4. pi-coding-agent
  5. pi-mom
  6. pi-web-ui
  7. pi-pods
```

---

## 五、分模块架构图

### 5.1 pi-ai — 统一 LLM API 层

```
+================================================================+
|                     @mariozechner/pi-ai                         |
|              统一多厂商 LLM API 抽象层                          |
+================================================================+
|                                                                 |
|  入口: src/index.ts (库), src/cli.ts (CLI 工具: pi-ai)         |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                    核心模块                              │    |
|  │                                                         │    |
|  │  types.ts ──────── 类型定义 & JSON Schema               │    |
|  │  models.ts ─────── 模型定义与配置                       │    |
|  │  models.generated.ts ── 自动生成的模型目录(309KB)       │    |
|  │  api-registry.ts ── Provider 注册表                     │    |
|  │  stream.ts ──────── 流式响应处理                        │    |
|  │  env-api-keys.ts ── API Key 环境变量管理                │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                Provider 适配器层                         │    |
|  │                  src/providers/                          │    |
|  │                                                         │    |
|  │  ┌───────────────┐ ┌───────────────┐ ┌──────────────┐  │    |
|  │  │   Anthropic   │ │    OpenAI     │ │    Google    │  │    |
|  │  │ anthropic.ts  │ │ openai-       │ │ google.ts    │  │    |
|  │  │               │ │ responses.ts  │ │ google-      │  │    |
|  │  │               │ │ openai-       │ │ gemini-      │  │    |
|  │  │               │ │ completions.ts│ │ cli.ts       │  │    |
|  │  └───────────────┘ └───────────────┘ │ google-      │  │    |
|  │                                      │ vertex.ts    │  │    |
|  │  ┌───────────────┐ ┌───────────────┐ │ google-      │  │    |
|  │  │ AWS Bedrock   │ │ Azure OpenAI  │ │ shared.ts    │  │    |
|  │  │ amazon-       │ │ azure-openai- │ └──────────────┘  │    |
|  │  │ bedrock.ts    │ │ responses.ts  │                    │    |
|  │  └───────────────┘ └───────────────┘ ┌──────────────┐  │    |
|  │                                      │   Mistral    │  │    |
|  │  ┌───────────────┐ ┌───────────────┐ │ mistralai.ts │  │    |
|  │  │ OpenAI Codex  │ │  通用工具     │ └──────────────┘  │    |
|  │  │ openai-codex- │ │ transform-    │                    │    |
|  │  │ responses.ts  │ │ messages.ts   │                    │    |
|  │  │               │ │ register-     │                    │    |
|  │  │               │ │ builtins.ts   │                    │    |
|  │  └───────────────┘ └───────────────┘                    │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                   工具模块 src/utils/                    │    |
|  │                                                         │    |
|  │  validation.ts · json-parse.ts · http-proxy.ts          │    |
|  │  event-streaming.ts · overflow-handling.ts               │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  外部依赖:                                                      |
|  @anthropic-ai/sdk · openai · @google/genai                     |
|  @aws-sdk/client-bedrock-runtime · @mistralai/mistralai         |
|  @sinclair/typebox · ajv · chalk · partial-json                 |
|  proxy-agent · undici · zod-to-json-schema                      |
+================================================================+
```

### 5.2 pi-agent-core — Agent 运行时

```
+================================================================+
|                  @mariozechner/pi-agent-core                    |
|              通用 Agent 运行时框架                              |
+================================================================+
|                                                                 |
|  入口: src/index.ts                                             |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                    核心模块                              │    |
|  │                                                         │    |
|  │  agent.ts ───────── Agent 主类                          │    |
|  │                     · 创建与配置                         │    |
|  │                     · 工具注册                           │    |
|  │                     · 生命周期管理                       │    |
|  │                                                         │    |
|  │  agent-loop.ts ──── Agent 执行循环                      │    |
|  │                     · LLM 调用编排                      │    |
|  │                     · 工具调用处理                       │    |
|  │                     · 迭代控制                           │    |
|  │                                                         │    |
|  │  proxy.ts ───────── Agent 代理工具                      │    |
|  │                     · 子 Agent 代理                     │    |
|  │                     · 消息转发                           │    |
|  │                                                         │    |
|  │  types.ts ───────── 类型定义                            │    |
|  │                     · AgentConfig                       │    |
|  │                     · AgentState                        │    |
|  │                     · Tool 接口                         │    |
|  │                     · Transport 接口                    │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  依赖: @mariozechner/pi-ai                                     |
|                                                                 |
|  ┌─────────────────────┐      ┌────────────────────────┐       |
|  │   Agent             │      │    pi-ai               │       |
|  │   · 接收用户消息    │─────>│    · 调用 LLM          │       |
|  │   · 执行工具        │      │    · 流式响应          │       |
|  │   · 返回结果        │<─────│    · 解析结果          │       |
|  └─────────────────────┘      └────────────────────────┘       |
+================================================================+
```

### 5.3 pi-coding-agent — 编程 Agent (主应用)

```
+================================================================+
|                 @mariozechner/pi-coding-agent                   |
|            交互式编程 Agent CLI (命令: pi)                      |
+================================================================+
|                                                                 |
|  入口: src/cli.ts (CLI) · src/index.ts (库) · src/main.ts     |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │               CLI 层 · src/cli/                         │    |
|  │                                                         │    |
|  │  args.ts ──────────── 命令行参数解析                    │    |
|  │  session-picker.ts ── 会话选择器                        │    |
|  │  config-selector.ts ─ 配置选择器                        │    |
|  │  list-models.ts ───── 模型列表                          │    |
|  │  file-processor.ts ── 文件处理                          │    |
|  └─────────────────────────────────────────────────────────┘    |
|                            │                                    |
|                            v                                    |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │            核心逻辑 · src/core/                         │    |
|  │                                                         │    |
|  │  agent-session.ts ─── 会话管理 (创建/恢复/持久化)      │    |
|  │  session-manager.ts ─ 会话文件管理                      │    |
|  │  settings-manager.ts ─ 用户设置                         │    |
|  │  model-registry.ts ── 模型注册表                        │    |
|  │  prompt-templates.ts ─ 提示词模板                       │    |
|  │  bash-executor.ts ─── Bash 命令执行器                   │    |
|  │  package-manager.ts ── 包管理器检测                     │    |
|  │  resource-loader.ts ── 资源加载                         │    |
|  │  diagnostics.ts ───── 诊断信息                          │    |
|  │                                                         │    |
|  │  ┌───────────────────────────────────────────────┐      │    |
|  │  │          工具系统 · src/core/tools/            │      │    |
|  │  │                                               │      │    |
|  │  │  read.ts ───── 文件读取     bash.ts ── Shell  │      │    |
|  │  │  write.ts ──── 文件写入     find.ts ── 搜索   │      │    |
|  │  │  edit.ts ───── 文件编辑     grep.ts ── Grep   │      │    |
|  │  │  edit-diff.ts ─ Diff 编辑   ls.ts ──── 列目录 │      │    |
|  │  │  truncate.ts ── 截断处理                      │      │    |
|  │  │  path-utils.ts ─ 路径工具                     │      │    |
|  │  └───────────────────────────────────────────────┘      │    |
|  │                                                         │    |
|  │  ┌───────────────────────────────────────────────┐      │    |
|  │  │       扩展系统 · src/core/extensions/          │      │    |
|  │  │                                               │      │    |
|  │  │  loader.ts ── 动态加载扩展 (.pi/extensions)   │      │    |
|  │  │  runner.ts ── 执行扩展代码                    │      │    |
|  │  │  wrapper.ts ─ 安全包装 / 上下文注入           │      │    |
|  │  │  types.ts ─── 扩展接口定义                    │      │    |
|  │  └───────────────────────────────────────────────┘      │    |
|  │                                                         │    |
|  │  ┌───────────────────────────────────────────────┐      │    |
|  │  │       会话压缩 · src/core/compaction/          │      │    |
|  │  │       HTML 导出 · src/core/export-html/        │      │    |
|  │  └───────────────────────────────────────────────┘      │    |
|  └─────────────────────────────────────────────────────────┘    |
|                            │                                    |
|                            v                                    |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │            UI 模式 · src/modes/                         │    |
|  │                                                         │    |
|  │  ┌────────────────────┐  ┌─────────────┐               │    |
|  │  │ interactive/       │  │ rpc/        │               │    |
|  │  │ (交互式终端 UI)    │  │ (RPC 模式)  │               │    |
|  │  │ ·组件系统          │  │ ·外部集成   │               │    |
|  │  │ ·主题支持 (JSON)   │  │             │               │    |
|  │  └────────────────────┘  └─────────────┘               │    |
|  │  ┌────────────────────┐                                 │    |
|  │  │ print-mode.ts      │                                 │    |
|  │  │ (非交互输出模式)   │                                 │    |
|  │  └────────────────────┘                                 │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │            工具函数 · src/utils/                        │    |
|  │                                                         │    |
|  │  git.ts · shell.ts · clipboard.ts · image-*.ts          │    |
|  │  changelog.ts · tools-manager.ts · mime.ts              │    |
|  │  frontmatter.ts · sleep.ts                              │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  内部依赖: pi-ai · pi-agent-core · pi-tui                      |
|  外部依赖: jiti · chalk · diff · glob · ignore · marked ·      |
|           minimatch · yaml · file-type · cli-highlight ·        |
|           proper-lockfile · photon-node                         |
|  可选依赖: @mariozechner/clipboard                              |
+================================================================+
```

### 5.4 pi-tui — 终端 UI 库

```
+================================================================+
|                     @mariozechner/pi-tui                        |
|               终端 UI 库 · 差分渲染引擎                        |
+================================================================+
|                                                                 |
|  入口: src/index.ts                                             |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                    核心引擎                              │    |
|  │                                                         │    |
|  │  tui.ts ──────────── TUI 主类 (39KB)                    │    |
|  │                      · 差分渲染引擎                     │    |
|  │                      · 组件树管理                       │    |
|  │                      · 布局计算                         │    |
|  │                                                         │    |
|  │  terminal.ts ──────── 终端控制 (ANSI 序列)              │    |
|  │  terminal-image.ts ── 终端图片渲染                      │    |
|  │  keys.ts ─────────── 按键处理 (34KB)                    │    |
|  │  keybindings.ts ──── 快捷键绑定管理                     │    |
|  │  stdin-buffer.ts ─── 输入缓冲区                         │    |
|  │  autocomplete.ts ─── 自动补全 (20KB)                    │    |
|  │  fuzzy.ts ────────── 模糊匹配算法                       │    |
|  │  utils.ts ────────── 工具函数 (24KB)                    │    |
|  │  editor-component.ts ── 编辑器组件核心                  │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │               组件库 · src/components/                  │    |
|  │                                                         │    |
|  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐  │    |
|  │  │ box.ts   │ │ text.ts  │ │ input.ts │ │ editor.ts │  │    |
|  │  │ 布局容器 │ │ 文本显示 │ │ 输入框   │ │ 文本编辑  │  │    |
|  │  └──────────┘ └──────────┘ └──────────┘ └───────────┘  │    |
|  │  ┌──────────┐ ┌──────────────┐ ┌──────────────────┐    │    |
|  │  │markdown  │ │ select-list  │ │ settings-list    │    │    |
|  │  │.ts       │ │ .ts          │ │ .ts              │    │    |
|  │  │Markdown  │ │ 选择列表     │ │ 设置列表         │    │    |
|  │  │渲染      │ │              │ │                  │    │    |
|  │  └──────────┘ └──────────────┘ └──────────────────┘    │    |
|  │  ┌──────────┐ ┌──────────────────┐ ┌──────────┐        │    |
|  │  │image.ts  │ │cancellable-      │ │spacer.ts │        │    |
|  │  │图片组件  │ │loader.ts         │ │间距组件  │        │    |
|  │  │          │ │可取消加载指示器  │ │          │        │    |
|  │  └──────────┘ └──────────────────┘ └──────────┘        │    |
|  │  ┌──────────────────┐                                   │    |
|  │  │ truncated-text.ts│                                   │    |
|  │  │ 截断文本显示     │                                   │    |
|  │  └──────────────────┘                                   │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  外部依赖: chalk · marked · mime-types · get-east-asian-width   |
|  无内部依赖 (独立基础库)                                        |
+================================================================+
```

### 5.5 pi-mom — Slack 机器人

```
+================================================================+
|                     @mariozechner/pi-mom                        |
|                  Slack 聊天机器人                                |
+================================================================+
|                                                                 |
|  入口: src/main.ts (CLI: mom)                                  |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                    核心模块                              │    |
|  │                                                         │    |
|  │  main.ts ───── 入口 / Slack 连接初始化 (9KB)            │    |
|  │  agent.ts ──── Agent 编排 / 消息处理 (31KB)             │    |
|  │  slack.ts ──── Slack API 集成 (18KB)                    │    |
|  │  events.ts ─── 事件处理与路由 (10KB)                    │    |
|  │  context.ts ── 上下文管理 (8KB)                         │    |
|  │  sandbox.ts ── 代码沙箱执行 (5KB)                       │    |
|  │  store.ts ──── 数据持久化 (6KB)                         │    |
|  │  download.ts ─ 文件下载处理 (3KB)                       │    |
|  │  log.ts ────── 日志系统 (9KB)                           │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │              工具系统 · src/tools/                      │    |
|  │                                                         │    |
|  │  read.ts · write.ts · edit.ts · bash.ts                 │    |
|  │  attach.ts (附件处理) · truncate.ts (截断)              │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │                   数据流                                 │    |
|  │                                                         │    |
|  │  Slack ──> events.ts ──> agent.ts ──> pi-coding-agent   │    |
|  │    ^                                       │             │    |
|  │    │                                       │             │    |
|  │    └───── slack.ts <── 结果格式化 <────────┘             │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  内部依赖: pi-ai · pi-agent-core · pi-coding-agent             |
|  外部依赖: @slack/socket-mode · @slack/web-api ·               |
|           @anthropic-ai/sandbox-runtime · @sinclair/typebox ·   |
|           chalk · croner · diff                                 |
+================================================================+
```

### 5.6 pi-web-ui — Web UI 组件库

```
+================================================================+
|                    @mariozechner/pi-web-ui                      |
|             Web UI 组件库 · AI 聊天界面                        |
+================================================================+
|                                                                 |
|  入口: src/index.ts (库) · dist/app.css (样式)                 |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │            主要组件 · src/components/                   │    |
|  │                                                         │    |
|  │  ChatPanel.ts ────────── 主聊天面板                     │    |
|  │  Messages.ts ─────────── 消息容器                       │    |
|  │  MessageList.ts ──────── 消息列表                       │    |
|  │  StreamingMessageContainer.ts ── 流式消息               │    |
|  │  MessageEditor.ts ────── 消息编辑器                     │    |
|  │  Input.ts ────────────── 输入组件                       │    |
|  │  AgentInterface.ts ───── Agent 交互界面                 │    |
|  │  ConsoleBlock.ts ─────── 控制台输出块                   │    |
|  │  ThinkingBlock.ts ────── 思考过程展示块                 │    |
|  │  AttachmentTile.ts ───── 附件展示磁贴                   │    |
|  │  ExpandableSection.ts ── 可展开区域                     │    |
|  │  ProviderKeyInput.ts ─── Provider 密钥输入              │    |
|  │  CustomProviderCard.ts ─ 自定义 Provider 卡片           │    |
|  │  SandboxedIframe.ts ──── 沙箱 iframe                   │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │  工具/存储/提示词                                       │    |
|  │                                                         │    |
|  │  src/tools/ ──── 工具类型 · JS REPL · 渲染注册表       │    |
|  │  src/storage/ ── 本地存储 · 应用状态管理                │    |
|  │  src/prompts/ ── 提示词模板                             │    |
|  │  src/dialogs/ ── 对话框组件                             │    |
|  │  src/utils/ ──── 附件 · 格式化 · 模型发现 ·            │    |
|  │                  代理 · 国际化 · 认证                   │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │  示例应用: example/                                     │    |
|  │  · Vite 开发服务器                                      │    |
|  │  · 展示如何集成 pi-web-ui 组件                          │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  内部依赖: pi-ai · pi-tui                                      |
|  Peer 依赖: @mariozechner/mini-lit · lit                       |
|  外部依赖: @lmstudio/sdk · ollama · lucide · jszip ·           |
|           docx-preview · pdfjs-dist · xlsx                      |
+================================================================+
```

### 5.7 pi-pods — GPU Pod 管理

```
+================================================================+
|                      @mariozechner/pi                           |
|               vLLM 部署 GPU Pod 管理 CLI                       |
+================================================================+
|                                                                 |
|  入口: src/cli.ts (CLI: pi-pods)                               |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │               CLI 命令 · src/commands/                  │    |
|  │                                                         │    |
|  │  pods.ts ───── Pod 管理 (创建/启动/停止/销毁)          │    |
|  │  models.ts ─── 模型管理 (部署/配置/查看)               │    |
|  │  prompt.ts ─── 交互式提示                               │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
|  ┌─────────────────────────────────────────────────────────┐    |
|  │               核心模块                                   │    |
|  │                                                         │    |
|  │  config.ts ──────── 配置管理                            │    |
|  │  types.ts ───────── 类型定义                            │    |
|  │  ssh.ts ─────────── SSH 连接管理                        │    |
|  │  model-configs.ts ── 模型配置信息                       │    |
|  │  models.json ────── 模型配置数据 (7KB)                  │    |
|  └─────────────────────────────────────────────────────────┘    |
|                                                                 |
+================================================================+
|  内部依赖: pi-agent-core                                        |
|  外部依赖: chalk                                                |
+================================================================+
```

---

## 六、完整依赖树

### 6.1 内部依赖树

```
pi-mono (monorepo root)
│
├── @mariozechner/pi-tui@0.51.5          [独立基础库]
│   └── (无内部依赖)
│
├── @mariozechner/pi-ai@0.51.5           [独立基础库]
│   └── (无内部依赖)
│
├── @mariozechner/pi-agent-core@0.51.5
│   └── @mariozechner/pi-ai
│
├── @mariozechner/pi-coding-agent@0.51.5
│   ├── @mariozechner/pi-ai
│   ├── @mariozechner/pi-agent-core
│   │   └── @mariozechner/pi-ai          (传递依赖, 已去重)
│   └── @mariozechner/pi-tui
│
├── @mariozechner/pi-mom@0.51.5
│   ├── @mariozechner/pi-ai
│   ├── @mariozechner/pi-agent-core
│   │   └── @mariozechner/pi-ai          (传递依赖, 已去重)
│   └── @mariozechner/pi-coding-agent
│       ├── @mariozechner/pi-ai          (传递依赖, 已去重)
│       ├── @mariozechner/pi-agent-core  (传递依赖, 已去重)
│       └── @mariozechner/pi-tui         (传递依赖)
│
├── @mariozechner/pi-web-ui@0.51.5
│   ├── @mariozechner/pi-ai
│   └── @mariozechner/pi-tui
│
└── @mariozechner/pi@0.51.5 (pods)
    └── @mariozechner/pi-agent-core
        └── @mariozechner/pi-ai          (传递依赖)
```

### 6.2 外部依赖树

```
pi-mono (monorepo root)
│
├─ 根级依赖
│  ├── @biomejs/biome (代码检查/格式化)
│  ├── @typescript/native-preview (tsgo 编译器)
│  ├── concurrently (并行脚本)
│  ├── husky (Git hooks)
│  ├── tsx (TypeScript 执行器)
│  └── typescript
│
├─ @mariozechner/pi-tui
│  ├── chalk (终端着色)
│  ├── marked (Markdown 解析)
│  ├── mime-types (MIME 类型)
│  └── get-east-asian-width (东亚字符宽度)
│
├─ @mariozechner/pi-ai
│  ├── @anthropic-ai/sdk ────────── Anthropic Claude API
│  ├── openai ───────────────────── OpenAI API
│  ├── @google/genai ────────────── Google Gemini API
│  ├── @aws-sdk/client-bedrock-runtime ── AWS Bedrock API
│  ├── @mistralai/mistralai ─────── Mistral API
│  ├── @sinclair/typebox ────────── JSON Schema 类型
│  ├── ajv / ajv-formats ────────── JSON Schema 验证
│  ├── zod-to-json-schema ───────── Zod → JSON Schema
│  ├── chalk ────────────────────── 终端着色
│  ├── partial-json ─────────────── 部分 JSON 解析
│  ├── proxy-agent ──────────────── HTTP 代理
│  └── undici ───────────────────── HTTP 客户端
│
├─ @mariozechner/pi-agent-core
│  └── (仅内部依赖 pi-ai)
│
├─ @mariozechner/pi-coding-agent
│  ├── @mariozechner/jiti ───────── 动态 TS/ESM 加载
│  ├── @silvia-odwyer/photon-node ── 图片处理 (WASM)
│  ├── chalk ────────────────────── 终端着色
│  ├── cli-highlight ────────────── 语法高亮
│  ├── diff ─────────────────────── 文本差异比较
│  ├── file-type ────────────────── 文件类型检测
│  ├── glob ─────────────────────── 文件模式匹配
│  ├── ignore ───────────────────── .gitignore 解析
│  ├── marked ───────────────────── Markdown 解析
│  ├── minimatch ────────────────── 路径匹配
│  ├── proper-lockfile ──────────── 文件锁
│  ├── yaml ─────────────────────── YAML 解析
│  └── @mariozechner/clipboard ──── 剪贴板 (可选)
│
├─ @mariozechner/pi-mom
│  ├── @slack/socket-mode ───────── Slack Socket API
│  ├── @slack/web-api ───────────── Slack Web API
│  ├── @anthropic-ai/sandbox-runtime ── 沙箱运行时
│  ├── @sinclair/typebox ────────── JSON Schema 类型
│  ├── chalk ────────────────────── 终端着色
│  ├── croner ───────────────────── 定时任务
│  └── diff ─────────────────────── 文本差异比较
│
├─ @mariozechner/pi-web-ui
│  ├── @lmstudio/sdk ────────────── LM Studio 集成
│  ├── ollama ───────────────────── Ollama 集成
│  ├── lucide ───────────────────── 图标库
│  ├── jszip ────────────────────── ZIP 处理
│  ├── docx-preview ─────────────── Word 文档预览
│  ├── pdfjs-dist ───────────────── PDF 渲染
│  ├── xlsx ─────────────────────── Excel 处理
│  ├── @mariozechner/mini-lit ───── Web Components (peer)
│  └── lit ──────────────────────── Web Components (peer)
│
└─ @mariozechner/pi (pods)
   └── chalk ────────────────────── 终端着色
```

---

## 七、数据流架构

```
+================================================================+
|                       典型数据流                                |
+================================================================+

  用户输入
    │
    ├──> [终端] ──> pi-coding-agent ──> pi-agent-core ──> pi-ai
    │                    │                                  │
    │                    │ 工具调用                         │ LLM 请求
    │                    v                                  v
    │              ┌──────────┐                    ┌──────────────┐
    │              │ read     │                    │  Anthropic   │
    │              │ write    │                    │  OpenAI      │
    │              │ edit     │                    │  Google      │
    │              │ bash     │                    │  Bedrock     │
    │              │ find     │                    │  Mistral     │
    │              │ grep     │                    │  Azure       │
    │              │ ls       │                    │  Vertex      │
    │              └──────────┘                    └──────────────┘
    │                                                   │
    │                                                   v
    │                                            流式响应返回
    │                                                   │
    │              pi-tui (终端渲染) <──────────────────┘
    │                    │
    v                    v
  用户看到输出 <── 差分渲染到终端

  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─

  Slack 消息流:

  Slack 用户
    │
    v
  @slack/socket-mode ──> pi-mom (events.ts)
                              │
                              v
                         agent.ts ──> pi-coding-agent
                              │            │
                              v            v
                         slack.ts    pi-agent-core ──> pi-ai
                              │                         │
                              v                         v
                         格式化回复              LLM Provider
                              │
                              v
                       Slack 频道/线程

  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─

  Web UI 数据流:

  浏览器用户
    │
    v
  pi-web-ui (ChatPanel)
    │
    ├──> Input 组件 ──> 消息发送
    │                      │
    │                      v
    │              pi-ai (直接调用 LLM)
    │                      │
    │                      v
    │              流式响应 (StreamingMessageContainer)
    │                      │
    v                      v
  MessageList ──> 渲染消息到 DOM
```

---

## 八、技术栈总览

```
+================================================================+
|                        技术栈                                   |
+================================================================+
|                                                                 |
|  语言与运行时                                                   |
|  ├── TypeScript (ESM 模块)                                     |
|  ├── Node.js >= 20.0.0                                         |
|  └── tsgo (TypeScript Go 编译器)                               |
|                                                                 |
|  Monorepo 管理                                                  |
|  ├── npm workspaces                                            |
|  ├── 版本同步脚本 (scripts/sync-versions.js)                   |
|  └── 串行构建链                                                 |
|                                                                 |
|  代码质量                                                       |
|  ├── Biome (Lint + Format)                                     |
|  ├── Husky (Git Hooks)                                         |
|  ├── Vitest (单元测试)                                         |
|  └── TypeScript 严格类型检查                                    |
|                                                                 |
|  AI / LLM                                                       |
|  ├── Anthropic Claude (anthropic-ai/sdk)                       |
|  ├── OpenAI GPT / Codex (openai)                               |
|  ├── Google Gemini / Vertex (@google/genai)                    |
|  ├── AWS Bedrock (@aws-sdk/client-bedrock-runtime)             |
|  ├── Mistral (@mistralai/mistralai)                            |
|  └── Azure OpenAI                                              |
|                                                                 |
|  UI                                                             |
|  ├── 终端: 自研 pi-tui (差分渲染)                              |
|  ├── Web: Lit + mini-lit (Web Components)                      |
|  ├── 样式: Tailwind CSS                                        |
|  └── 图标: Lucide                                              |
|                                                                 |
|  集成                                                           |
|  ├── Slack (@slack/socket-mode + web-api)                      |
|  ├── LM Studio (@lmstudio/sdk)                                 |
|  └── Ollama (ollama)                                           |
|                                                                 |
+================================================================+
```

---

> 本文档由项目源码自动分析生成，最后更新: 2026-02-04
